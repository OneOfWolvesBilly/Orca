spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:orca;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

  flyway:
    clean-disabled: false
