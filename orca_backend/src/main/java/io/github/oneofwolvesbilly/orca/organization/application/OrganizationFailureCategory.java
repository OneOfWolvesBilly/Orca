package io.github.oneofwolvesbilly.orca.organization.application;

/** Stable organization-owned meaning for expected application failures. */
public enum OrganizationFailureCategory {
    NOT_FOUND,
    FORBIDDEN,
    APPLICATION_REJECTED
}
