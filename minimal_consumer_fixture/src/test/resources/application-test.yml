spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:orca_fixture;MODE=MySQL;DATABASE_TO_UPPER=false;DB_CLOSE_DELAY=-1
    username: sa
    password: sa

  flyway:
    enabled: true
    locations: classpath:db/migration
    clean-disabled: false
